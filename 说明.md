1. 先切 Tab：找 .switch-tab-item 中包含 "包年" 的那个，如果不是当前 active 就点击切换
  2. 再找卡片：在切换后的卡片列表里找文本以 "Lite" 开头的那张
  3. 点击按钮：点击该卡片内未禁用的订阅按钮

  改套餐只需修改 config.py 里两行：
  PLAN_NAME = "Lite"   # 改 Pro / Max
  PLAN_PERIOD = "包年"  # 改 包月 / 包季